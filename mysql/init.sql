CREATE DATABASE biblioteca;

USE biblioteca;

CREATE TABLE libro (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    Nombre VARCHAR(100)
    Telefono VARCHAR(10)
    Correo VARCHAR (30)
    Rol VARCHAR (50)
);
